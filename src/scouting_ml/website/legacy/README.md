## Legacy Generated Frontend

This directory contains the original generated frontend and its build helpers.

It is archived reference code only.

- It is not the canonical ScoutML frontend.
- It is not wired into the FastAPI-backed `market-value` workflow.
- The active backend-connected UI lives under `src/scouting_ml/website/static/`.

Keep this folder only as historical reference while the static recruitment UI remains the supported path.
